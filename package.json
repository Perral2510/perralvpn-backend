{
  "name": "perralvpn-backend",
  "version": "1.0.0",
  "private": true,
  "description": "PerralVPN backend API with 3x-ui provisioning",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "check": "node --check backend/server.js && node --check backend/xui.js && node --check backend/xui-provision.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "express": "^5.2.1",
    "nodemailer": "^9.0.5",
    "qrcode": "^1.5.4",
    "sepay-pg-node": "^1.0.0"
  }
}
