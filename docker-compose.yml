services:
  app:
    build: .
    container_name: ducanhvpn-app
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ducanhvpn-data:/app/data
    ports:
      - "127.0.0.1:3000:3000"

volumes:
  ducanhvpn-data:
